<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jcode.loomzy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Loomzy">
        <activity android:name=".Add_vehicle"></activity>
        <activity android:name=".login" />
        <activity android:name=".bookservice.BookServiceActivity" />
        <activity android:name=".user_home" />
        <activity android:name=".signup_address" />
        <activity android:name=".signup" />
        <activity android:name=".Servicer_home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>